<%- include('../layout', { 
    title: typeof title !== 'undefined' ? title : 'Vehicle Makes - GLM Admin',
    page: typeof page !== 'undefined' ? page : 'vehicle-makes',
    body: `
    <div class="header">
        <h1>Vehicle Makes</h1>
        <div class="header-actions">
            <button class="btn btn-primary">Add Vehicle Make</button>
        </div>
    </div>

    <div class="content-card">
        <div class="empty-state">
            <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4" />
            </svg>
            <h3>Vehicle Makes Management</h3>
            <p>Manage vehicle makes (Toyota, Honda, etc.) and their models. Database integration coming soon.</p>
        </div>
    </div>
` }) %>
